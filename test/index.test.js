const index = require("../index");

describe("index", () => {
    describe("HTML", () => {
      it("should see if HTML was generated", () => {
        // fill out testing parameters

      });
    });

    describe("Git Hub User", () => {
        it('should see if the Git hub username was applied to the object', () => {
            //fill out testing parameters
        });
    });

    describe("Git Hub User tab", () => {
        it('should see if the Git hub user was opened in a new tab', () => {
            //fill out testing parameters this should only test engineers
        });
    });

    describe("role", () => {
        it('should see if the role (i.e. employee, intern, engineer, team manager) was applied to the object', () => {
            //fill out testing parameters
        });
    });

    describe("Email", () => {
        it('should see if the email was applied to the object', () => {
            //fill out testing parameters
        });
    });

    describe('TeamManager', () => {
        it('should see if the team manager name is applied to the object', () => {
            //fill out testing parameters
        });
    });

    describe('ID', () => {
        it('should see if the ID number is applied to the object', () => {
            //fill out testing parameters
        });
    });

    describe('officeNumber', () => {
        it('should see if the office number is applied to the object', () => {
            //fill out testing parameters
        });
    });

});
